# Copia este archivo como .env (en la misma carpeta que server.js) y pon
# aquí la contraseña real del panel. Este archivo .env.example SÍ se sube
# al repo como referencia; el .env real NUNCA debe subirse (ver .gitignore).

PANEL_PASSWORD=pon_aqui_la_contraseña_del_panel
